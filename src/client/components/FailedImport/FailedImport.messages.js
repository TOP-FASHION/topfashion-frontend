import { defineMessages } from 'react-intl'

export default defineMessages({
  'app.component.failedImport.text': {
    id: 'app.component.failedImport.text',
    defaultMessage:
      'Component cannot be loaded due to a connectivity problem. Please try to reload the page.'
  }
})
